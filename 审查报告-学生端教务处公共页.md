# 微课平台【学生端 + 教务处 + 公共页】静态代码审查报告

审查方式：60+ 文件全量读取（约 28,400 行），14 组并行审查 + 逐条对后端 Controller/DTO/Entity/SQL 交叉验证。以下均为有代码证据的确凿问题。

## P0（页面/核心功能不可用）

1. **[views/student/MicroSpecialtyDetail.vue:357 及 397-404/468/495/499/510/516/547/553/561/566-573] useI18n() 解构改名后残留裸 t(...) 调用 → ReferenceError**
   - `const { t: i18nT } = useI18n()` 后，脚本内 20+ 处仍调用未定义的 `t(...)`（statusLabel computed、handleApply/handleReapply/goCourse 等），文件内无 `const t` 定义，auto-imports.d.ts 仅注入 ElMessage、main.js 无全局 t。
   - 触发条件：详情加载成功渲染 `{{ statusLabel }}`（模板:76）即抛 ReferenceError；点报名/继续学习/进入课程任一按钮同样崩溃。
   - 业务影响：详情页渲染失败卡骨架屏；报名、重新申请、课程跳转全部失效（部分调用点被 try/catch 吞掉表现为按钮无响应）。
   - 根因：重命名 t → i18nT 时未全局替换（改名只改了一处）。

2. **[views/student/MyCourses.vue:861] 调用未 import 的 getLearningProgress → 课件类课程"继续学习"静默失效**
   - :522 只 import 了 `getCompletion, batchGetLearningProgress`，:861 却调用 `getLearningProgress({ courseId })` → ReferenceError 被 :869 的 catch 吞掉，降级为无 chapterId 跳转。
   - 触发条件：课件类课程点"继续学习"（handleContinue）。
   - 业务影响：永远无法恢复上次章节，每次从第一章开始，且无任何错误提示（静默降级）。
   - 根因：import 清单遗漏 + catch 吞错掩盖。

## P1（客户可感知的功能缺陷）

### 学生端

3. **[views/student/StudentFavorites.vue:82 与 views/student/LearningView.vue:521] 取消收藏把"收藏记录主键"当 courseId 传给已废弃接口**
   - `cancelFavorite(item.id)` → `DELETE /api/favorites/{id}`；后端 CourseFavoriteController.java:77-80 明确标注该接口"已废弃，(id = courseId)"，正确用法是 `/favorites/record/{id}`（api/favorite.js:6 已有 removeFavoriteRecord，教师端 FavoriteList.vue:140 用的就是它）。
   - 触发条件：学生取消任一收藏。
   - 业务影响：删除 0 行（刷新后收藏"复活"）；若收藏记录 id 恰好等于某课程 id 会误删其它课程的收藏。
   - 根因：前端仍在用旧接口语义；两处调用点均未迁移。

4. **[views/student/SlidePlayer.vue:1610-1618] 学生主流程下 updateVideoProgress 恒早退 → video_progress 永不落库**
   - `if (!courseId.value || !sectionId.value || !isStudent.value) return`，而 sectionId（:1564）只取 props/route；学生入口（MyCourses:868/870、LearningCenter:603、CourseSquare:691、CourseDetail:742/744）均不带 sectionId。
   - 业务影响：G3-P0-5 的 SKIP_IF_KNOWN 服务端分支对学生主流程整体失效；:683-684 的 advanceToNextPage 已用 page?.sectionId 兜底，此处漏改。

5. **[views/student/SlidePlayer.vue:1572/1589] chapterId 为空时进度查重恒失败 → 重复进度记录无界累积**
   - `records.find(r => r.chapterId === Number(chapterId.value))`，主流程无 chapterId → Number(null)===0，而落库记录 chapterId 为 null（:1576 传 undefined）→ find 永不命中 → 每次进入播放器/翻到最后页都新建一条 learning_progress。
   - 根因：null/0 语义混淆 + MyCourses:864 用 records.filter(r => r.chapterId) 过滤掉 null 记录导致永远拼不出 chapterId 参数。

6. **[views/student/VideoPlayer.vue:844-859 + composables/useVideoCompletionFlow.js:32 + useVideoProgressFlow.js:49-78] 视频看完完成态不落库（仅本地 UI 勾选）**
7. **[views/student/VideoPlayer.vue:851 + composables/useVideoProgressFlow.js:62-95] 播放器进度流完全忽略 videoId，按 chapterId 读写 → 多视频章节进度串档、与学习页口径分裂（P1-C）**
   - videoId 仅用于 dedupKey（:48），createPayload/updatePayload/findExistingProgress 均不含 sectionId/videoId：`findExistingProgress` 按 chapterId find 首条 → 同章多视频时断点可能落到另一视频的记录上；且与学习页 LearningView 按 sectionId 精确读写的记录是两套口径，同一视频两入口断点不互通，播放器还可能用 chapter 级位置覆盖学习页写入的 section 级记录。
8. **[views/student/VideoPlayer.vue:896-915 + composables/useVideoLoadOrchestrator.js:20-22,58] 进度心跳"启动修复"是半成品：选项未传入，调用是 noop（P1-I）**
   - orchestrator :20-22 注释声称"VideoPlayer 传入 startVideoProgressHeartbeat"并已在 :58 调用，但 VideoPlayer.vue:896-915 的 options 里没有这项 → 默认 `() => {}` 死调用；心跳实际只靠 useVideoPageLifecycle 的 `watch(isPlayingRef)`（:47-53）启动，而 isPlaying 仅由 togglePlay 手动置位、video 未监听原生 play/pause → HLS 自动播放成功或系统级暂停/retryHls 内 video.pause() 时心跳不启动、进度不上报、控制栏状态失真。
   - @ended → reportProgress()（无 force）→ shouldPersist 里 `if (!force && video.paused) return false`（ended 时 paused=true）→ 终态不持久化；且 createPayload/updatePayload 从不携带 completed:true。
   - 触发条件：学生看完任一视频（路由 /student/courses/:id/play/:videoId）。
   - 业务影响：章节完成勾选、课程完成度、证书判定全部依赖 learning_progress.completed，刷新即失；对照 LearningView.markLessonComplete 用的是 {force:true, completed:true}。

7. **[views/student/LearningCenter.vue:810-811] 连续打卡天数永远显示 0**
   - 后端 GET /check-ins/streak 返回 R<Integer> 裸整数（CheckInController.java:45-46），前端却按对象解析 `streakRes?.data?.streakDays ?? streakRes?.data?.streak` → 恒 undefined → 0。

8. **[views/student/LearningCenter.vue:893-902] 正确率趋势图恒为全 0 的误导性图表**
   - 后端 getAccuracyTrend 返回 {date:"2026-08-19", accuracy:0-1}（ExerciseRecordServiceImpl.java:367/372），前端用 `tDay === "周一"` 做星期匹配（:896）恒不命中 → found 恒 undefined → 全 0，但仍置 accuracyMode=true 渲染"正确率趋势"。

9. **[views/student/Exams.vue:106-113/347/357] 已作答未通过的考试仍显示"参加考试"按钮 → 点击必被后端拒绝**
   - _attempted 计算后从未使用（模板只判 !_passed）；后端 assertExamNotResubmittable（ExerciseAnswerSubmitExecutor.java:212-222）对已作答考试强制拒绝（EXAM_ALREADY_SUBMITTED）。
   - 业务影响：重进答题提交必失败，且错误被拦截器泛化，用户看不到"考试已提交，不可重复作答"。

10. **[views/student/Exams.vue:334/351-356] 依赖后端不存在的字段 startTime → "未开始/已截止"状态机整体死代码**
- **[views/student/Exams.vue:389-399] 前置章节检查把"课时级进度行"当"章节完成"判定，且依赖行序（P1-I）**：getLearningProgress 返回按 (chapterId, sectionId) 拆分的课时级行，`find(p => Number(p.chapterId) === Number(prev.id))` 取该章节首条进度行单行判定 → 首行恰好为已完成课时则其余课时全未完成也算"章节完成"（前置约束形同虚设）；首行为未完成课时则即使后续课时已完成也拦截（误伤，且结果依赖 DB 返回顺序）。应以该章节全部课时行 completed 聚合判定。
    - /api/exams/my 返回 ExerciseVO（无 startTime 字段）→ examTime 恒 null → expired/notStarted 恒 false → "已截止"按钮、expired tab、notStartedTip 全部不可达（:331-333 注释自称的 P1-C 修复从未生效）。

11. **[views/student/CourseDetail.vue:763] 评价"加载更多"失败时清空整份评价列表**
    - `catch { ...; reviews.value = [] }` 无条件清空，append 模式下点"查看更多"失败 → 已加载评价全部消失、显示"暂无评价"，且无法恢复。

12. **[views/student/CourseDetail.vue:857/868] 提交评价/回复后复用过期 reviewPage → 列表跳到后面的页**
    - handleSubmitReview/handleSubmitReply 调 fetchReviews() 未重置 reviewPage（点过"查看更多"后 >=1）→ 列表只显示后几页，新提交的评价（在第 1 页）不可见。

13. **[views/student/Settings.vue:202/351] 模板引用未声明的 saveTimer → 保存按钮 loading/防重复提交失效**
    - 全文件只有 debounceTimer（:495），`:loading="!!saveTimer"` 恒 false → 保存按钮无 loading，连点可触发并发保存；每次渲染报 Vue 未定义属性警告。

14. **[views/student/Settings.vue:114-137/282-305/394-408] 免打扰时间选择器回显错误**
    - el-time-picker 同时绑 v-model="quietHoursStartDate"（Date ref，初始 new Date()）与 :value="settings.quietHoursStart"（字符串 22:00），且 value-format="HH:mm" 会把 v-model 改写为字符串。
    - 业务影响：已保存的 22:00/07:00 永不回显，显示当前时刻；Date/string 类型反复横跳；onQuietHours*Change 每次选择额外触发 handleSave 双写。

15. **[views/student/MyCourses.vue:680/605-614] getMyEnrollments() 不带 size → 后端默认 size=20，第 21+ 门课永不显示**
    - 前端在 20 条内做客户端分页（displayCourses:612-613 slice），totalDisplayElements 也基于截断列表 → Tab 徽标、分页 total 全错；移动端 H5 无分页控件（:266-500）→ 第 10+ 门不可达。

16. **[views/student/ExerciseTake.vue:1098] 超时自动交卷 duration 未钳制 → 后端必拒且重试死循环**
    - `const duration = elapsedTime.value || ...` 优先用墙钟 elapsedTime（:1009 基于 performance.now），切后台超时后 duration > timeLimit*60 → 后端 assertTimeLimitNotExceeded（ExerciseAnswerSubmitExecutor.java:186-188）拒绝（EXAM_TIME_EXPIRED）；重试复用同一 duration 永远失败 → 答案丢失。
    - 修复方向：Math.min(elapsedTime, timeLimit*60)。

17. **[views/student/WeeklyReport.vue:404/409-419] 周报"视频时长"恒 0、"练习数/正确率"实为终身累计**
    - ① CheckIn.duration 全后端无写入路径（updateDuration 声明于 CheckInService.java:15 但零调用，CheckInServiceImpl.java:83 恒置 0）→ c.duration 恒 0；且 :404 按秒/60、LearningCenter.vue:722 按分钟直加、:928 又按秒/60 —— 同一字段三种解释。
    - ② :417-418 无条件累加 enrollment 终身 exerciseCount/correctCount（注释自称"仅在周数据时累加"），周报数据实为累计值，AI 建议阈值失真。

18. **[components/profile/AchievementBadges.vue:64] 徽章匹配字段名与后端序列化不一致 → 所有已获徽章显示"未解锁"**
    - 前端 e.badgeType === b.badgeType，后端 GET /api/badges/my 返回 AchievementVO（AchievementVO.java:11-24）字段为 badgeCode + name（badgeName 被 @JsonProperty("name") 改名），响应中无 badgeType → find 永不命中。同项目 AchievementWall.vue:134 用 a.badgeCode === d.code 可佐证正确用法。

19. **[views/student/LearningCenter.vue:159/199/205、TrainingCenter.vue:40] enrollment.progress 0-1 与 0-100 量纲混用**
    - 后端同一字段两套解释：MicroSpecialtyProgressServiceImpl.java:135 `progress * 100.0`（按 0-1），EnrollmentLifecycleServiceImpl.java:436 `progress > 50.0` 退课拦截（按 0-100）；前端直接把原值当 0-100 渲染 el-progress/百分比。
    - 业务影响：教师手动录进度后，学习中心显示 0.x%，微专业进度被放大 100 倍；退课 50% 拦截阈值在 0-1 量纲下永远不触发。

20. **[views/student/MicroSpecialtyDetail.vue:114-121] focus=failed 是误导性占位**
    - route.query.focus==="failed" 时显示"以下是未通过考核的课程"告警（:117），但课程列表渲染的是全量 courses，无失败标记/过滤（上游 MyMicroSpecialties 点"查看不合格课程"跳入后误导）。

21. **[views/auth/Login.vue:258-259] 账号锁定（423）重复且矛盾的 toast**
    - request.js:192-193 拦截器已提示（后端文案/默认"30 分钟后重试"），组件 catch 再弹"15 分钟后再试" → 同一失败两条提示且时长矛盾（后端锁定为 15 分钟、IP 400 封禁才是 30 分钟），用户无法判断真实锁定时长。

### 教务处

22. **[views/academic/EnrollmentOverview.vue:83-89] 状态列用 row.status，后端字段为 enrollmentStatus（EnrollmentVO.java:24）→ 所有行状态显示 "-"**（兄弟页 EnrollmentList.vue 用 row.enrollmentStatus 可对照）。
23. **[views/academic/EnrollmentOverview.vue:97] prop="createdAt"，VO 只有 enrolledAt → 选课时间列恒空**。
24. **[views/academic/EnrollmentOverview.vue:147-149] fetchStats 用 i.status === "ENROLLED"/"PENDING"/"COMPLETED" → 字段名错 + 只取前 100 条 → 统计卡恒 0/失真**。
25. **[views/academic/Dashboard.vue:667] 热门课程传 sort: "studentCount,desc"，后端只认 sortBy/sortOrder（CourseController.java:78-79）→ 排序被静默丢弃，"热门"榜单名不副实**。
26. **[views/academic/Dashboard.vue:202] 预警表 prop="name"，后端字段是 courseTitle（CompletionWarningVO.java:9）→ 课程名列恒空**。
27. **[views/academic/Dashboard.vue:611-616/626] 参与率（0-1）与完成率（0-100）共用 max:100 轴**：后端 SQL participation_rate = 人数/总学生（0-1，CourseRepository.java:164）、completion_rate = ×100.0（:187）→ 参与率曲线贴 0，视觉上恒为 0。
28. **[views/academic/MicroSpecialtyCrossDeptReview.vue:24-28/110/119] 引用 MicroSpecialtyTeacher 实体不存在的字段（teacherName/microSpecialtyTitle/teacherCollege/specialtyCollege）→ 全表 4 列空白 + 确认弹窗显示「undefined」**（实体字段：microSpecialtyId/teacherId/role/inviteStatus 等）。
29. **[views/academic/MicroSpecialtyCrossDeptReview.vue:98/99] "全部" tab 与"待审批" tab 数据完全相同**：后端 getPendingCrossDeptInvites 不接收 inviteStatus 参数、硬编码只返回 PENDING_ACADEMIC（MicroSpecialtyInviteService.java:16-17）→ 审批后记录消失，"全部" tab 名存实亡。
- **[views/academic/MicroSpecialtyCrossDeptReview.vue:39 + MicroSpecialtyInviteServiceImpl.java:271-272] 对 inviteStatus="PENDING" 的行显示批准/驳回按钮，后端必拒（P1）**：列表查询硬编码包含 "PENDING"（impl:84），而 reviewCrossDept 唯一合法前置状态是 "PENDING_ACADEMIC"（impl:271-272，否则抛"非跨学院待审状态"）→ PENDING 行的两个操作按钮点击必现错误提示，按钮形同虚设。
30. **[views/academic/MicroSpecialtyClassImport.vue:101] 院系筛选用 c.departmentId/c.departmentName，ClassVO 中不存在（ClassVO.java:7-13 只有 majorId/majorName/grade）→ 选院系后班级列表恒空，导入功能被锁死**。
31. **[views/academic/MicroSpecialtyGoldManage.vue:24/31/32-33] collegeName/enrollmentCount/featuredAt 均不在 MicroSpecialtyVO（真实字段 departmentName/totalEnrollments）→ 3 列恒空白**；且后端全仓库无 totalGoldCount（grep 0 命中）→ :83-87 永远走"当前页统计"fallback，"上限 2"计数错误、按钮禁用逻辑错误（AC09 修复未生效）。
32. **[views/academic/MicroSpecialtyReview.vue] collegeName 列恒空白、详情弹窗恒 "-"（同类 VO 字段错配）**。

### 组件/其他

33. **[components/users/TeacherApprovalDialog.vue + BatchImportDialog.vue] 无任何打开入口，功能不可达**：teacherApprovalVisible/batchImportVisible 仅在 views/users/UserList.vue:288/294/350/353 定义与绑定，全项目无任何按钮将其置 true → 教师审核、批量导入用户是死功能。
34. **[components/course/SectionEditDialog.vue:73-80] 新增课时表单残留上一课时数据**：watch 只在 if (s) 时回填，父组件新增模式传 section=null（CourseDetail.vue:765）→ 编辑后点"新增"表单仍是旧值（标题/排序/时长），易误提交重复课时；弹窗常驻（无 v-if 销毁）使 form 状态跨开关保留。

## P2 / 体验

- [views/student/CourseSquare.vue:267] `<FolderOpened />` 未 import 且不在 main.js 74 个全局图标注册表（vite.config 仅 ElementPlusResolver，无 IconsResolver）→ 图标空白。
- [views/student/CourseDetail.vue:362] "查看更多"按钮只判 reviews.length >= 5，从不依据 reviewTotalElements 隐藏 → 加载完仍空转；:772-775 fetchRanking 是死功能却每次发请求、失败弹无对应 UI 的报错 toast。
- [views/student/BundleDetail.vue:7-36] el-col 固定 :span="16/8" 无 xs/sm 响应式 → 375px 下购买卡被压到 1/3 宽；:193-199 支付成功后刷新失败被静默吞掉 → 按钮状态错误可诱导重复下单。
- [views/student/DiscussionView.vue:362-374] 发帖时解析的 courseId 从未传给 createPost（纯死代码 + 每次发帖多发一次 getChapterById）；:304-313 切换章节未重置 page → 新章节请求越界、列表空白；:73 起 H5 发布按钮无 :disabled="!chapterId" 且 H5 无课程/章节选择器 → 直接访问 /student/discussions 提交 chapterId:null。
- [components/learning-view/NotesPanel.vue:32-40] "公告" tab 纯静态占位，全代码库无公告数据源（死 tab）；:116-127 笔记加载失败静默吞掉 → 保存成功但不显示造成假空态；:100 defineEmits(["change-tab"]) 从未 emit。
- [views/student/MyReviews.vue:306] ElMessageBox 关闭（"close"，点 X/Esc）落入 catch → 误弹"删除失败"；:86-96 PC 端错误态与空态叠加渲染；:322 handleSizeChange 不重置页码 → 改 page-size 后可能空白页。
- [views/student/MyReviews.vue:244-259] fetchCourseOptions 只取 getMyReviews({page:0,size:100}) 的前 100 条派生课程筛选项 → 评价 >100 条的用户筛选项不全（未用 fetchAllPages，与主列表逻辑不一致）。
- [views/student/MyReviews.vue:218 vs 234/331] isMobile 初始化用 `<= 768`、resize 回调用 `< 768`，断点不一致（正好 768px 时初始按移动、缩放后按 PC，行为跳变）。
- [views/student/MyReviews.vue:274] 注释"后端 getMyReviews(userId, page, size)"与实际 API 签名（getMyReviews(params) 对象参数，course-review.js:32）不符，误导维护。
- [views/student/MyReviews.vue:207/262/277] 死 import getReviews、死存储 allReviews（只赋值从未读取）。
- [views/student/Settings.vue:519/521/527] debouncedSave 先 ElMessage.success(savedSuccess)，随后 localStorage.setItem 抛 QuotaExceededError 时又弹 saveFailedLocal → 同一操作连弹成功+失败两条 toast。
- [views/student/Profile.vue:77/155] 模板 ref="avatarUploadCompRef"/"avatarUploadMobileRef" 未在 script 声明对应 ref（死引用，无功能影响）。
- [views/student/Profile.vue:24 vs 209] 移动端首屏双骨架屏：桌面骨架分支（el-col 16/8）未按 !isMobile 门控，与 :209 独立移动骨架同时渲染，375px 下两套骨架叠加。
- [components/profile/AchievementBadges.vue:34] earnedAt 原始 ISO 时间戳直出（修复 P1 后必现）；:38 allBadges 恒 3 项 → el-empty 空态死代码。
- [components/profile/CertificatesCard.vue:76-82] await downloadCertificate 之后才 window.open → 用户激活丢失，弹窗拦截高概率静默失败。
- [components/profile/WrongQuestionsCard.vue:114] 传 { userId } 与后端从 JWT 取用户相悖，且未传 size → 后端默认 20 条，课程筛选下拉最多覆盖 20 门课。
- [components/learning-center/AccuracyTrendChart.vue:82] 正确率模式 y 轴仍硬编码 "小时" 且无 max:100（父组件把 0-1 正确率塞进 hours 字段）；:23 aria-label 仍写"本周学习时长分布图"。
- [views/student/SlidePlayer.vue] P2 若干：下载后立即 revokeObjectURL（Firefox 偶发中断）、"0/0" 页计数、error 态播放按钮未禁用导致重复错误 toast。
- [components/learning-view/VideoSection.vue] :active 非合法 prop（倍速无高亮）、切换课时 playbackRate 不重新应用（UI 显示 1.5x 实际 1.0x）、bufferedPercent 除零 NaN。
- [views/public/MicroSpecialtySquare.vue:181-196] gold-row flex 无 wrap + 封面 min-width:200px → 375px 下 >=2 个金标卡片横向溢出；:138 isLoggedIn computed 直读 localStorage 无响应式；匿名访客打开公开页渲染在教职工 Layout 内（空菜单 + 管理 Header）。
- [views/academic/LearningAnalytics.vue:140-141] 趋势箭头读 data.completionTrend/accuracyTrend，后端 VO 无此字段 → 死 UI；:46 :key="dept.id"（应为 departmentId）→ 重复 key；:133-143 静默失败无空态。
- [views/student/StudentOfflineSession.vue:87-118] 裸日期字符串按 UTC 解析，非东八区时区会整日不显示签到按钮（条件性）。
- [views/student/ExerciseTake.vue] :963-965 60/30/10 秒警告窗口仅 2 秒宽（漂移/后台恢复整段跳过）；:815-828 自动开始考试依赖 getExercises 默认第 1 页 size=10，章节练习 >10 条时找不到考试；:264-270 retrySubmit 按钮不可达；:628 Loading 图标未 import。
- [views/student/MyCourses.vue:854-868] 退课按钮在进度>50% 时仍显示（后端会拒绝）。

- [views/student/MyMicroSpecialties.vue:331-348] handleDrop 无 loading/防重入（无 dropping 状态 ref，退出按钮未绑 :loading），确认后请求在途期间可重复点击提交 drop；与 handleReapply（:350-370 有 reapplying 状态）不一致。
- [views/student/MyMicroSpecialties.vue:372-387] viewCertificate 在 a.click() 后立即 URL.revokeObjectURL（:382）→ Firefox 下可能尚未开始下载即回收，证书下载偶发失败；应延迟回收。
- [views/student/MicroSpecialtyDetail.vue:448-463] checkEnrollment 失败仅 console.warn 无用户提示 → enrollmentStatus 保持 null，底部按钮错误回到"立即报名"态，学生可能重复报名（仅服务端兜底）。
- [views/student/MicroSpecialtyDetail.vue:321-330] 末位 CTA label 三目死分支：v-else 分支必然满足 isLoggedIn && isStudent，:329 的 !isStudent 分支与 RECRUITING 分支均不可达（冗余条件）。
- [views/student/WeeklyReport.vue:300] `checkedDates.has(d.toDateString())` 兜底永不命中（checkedDates 存 "YYYY-MM-DD"，toDateString() 产出 "Wed Aug 05 2026"），纯死代码。
- [views/student/MicroSpecialtyDetail.vue:9] 同元素 v-if="loading" 与 v-loading="loading" 冗余（v-if 已保证仅 loading 时渲染）。

- [views/student/DiscussionView.vue:478-483/506-508] handleSizeChange 的 fetchTimer（setTimeout 200ms）在 onUnmounted 未清理（:506 只移除 resize 监听）→ 改每页条数后 200ms 内离开页面会在已卸载组件上执行 fetchData。
- [views/student/DiscussionView.vue:219] 死 import：`import { swrCache } from "@/composables/useStaleWhileRevalidate"` 全文件无引用。
- [views/student/DiscussionView.vue:241 vs 248] isMobile 断点不一致：初始 `<= 768`、resize 回调 `< 768`（768px 精确边界行为跳变）。
- [views/student/DiscussionView.vue + composables/useUrlPagination.js] URL 分页仅单向同步：bindToQuery 只做 page→URL，URL→page 仅在 onMounted 初始化一次 → 浏览器前进/后退改变 URL 的 page 不重新请求列表，停留旧页。
- [views/student/DiscussionView.vue:190/415-425 + components/CommentNode.vue:43] 审核状态只拦主回复框、不拦评论内回复：pending/驳回帖的每条评论"回复"按钮仍可用且 handleReply 无状态守卫 → 与页面"审核通过后开放评论"提示矛盾（若后端不拦则数据状态不一致）。

- [components/profile/WrongQuestionsCard.vue:110-119] fetchMyEnrollments 失败 catch 静默（:116-118），且 userInfo 未加载时 `if (!userId) return` 也静默 → 课程筛选下拉空且无任何用户提示。
- [components/profile/AchievementBadges.vue:38] el-empty 空态死代码：allBadges computed 恒返回固定 3 项（:57-67），`allBadges.length === 0` 永不成立（且卡片设计永远展示 3 个锁定徽章，与"暂无成就数据"文案自相矛盾）。

- [components/learning-view/VideoSection.vue:268-277] retryVideo 未重置 bufferedPercent → 重试后进度条残留上一段缓冲宽度。
- [views/student/SlidePlayer.vue:1106] HTML 分段页部分 READY 时置 audioStatus=pending，但未像 PPT pending 分支（:1175-1181）调用 startPendingTimer() → "生成耗时较长"提示永不出现，两分支行为不一致。
- [views/student/SlidePlayer.vue:1210] `narrationStatus !== "AUDIO_READY"` 严格枚举比较：legacy 数据 narrationStatus 缺失或为 v1 旧值（:1175 兼容 PENDING/AUDIO_PENDING 两种拼写说明旧数据存在）时，即使 narrationAudioUrl 存在也不加载音频，显示"无讲解音频"。
- [views/student/SlidePlayer.vue:407/981/1090/231] segmentAudioMode 恒为 false 的死标志：:1090 只赋 false 从不置 true → :981 分支不可达、:231 disabled 条件中 `!segmentAudioMode &&` 为恒真死代码。
- [views/student/SlidePlayer.vue:738-744] postMessage 校验空窗：PPT 页无 iframe 时 `if (iframe && event.source !== iframe.contentWindow)` 跳过 source 校验，仅凭 origin==="null" 放行；页面内存在其它 sandbox iframe 时可注入播放/暂停/跳转指令（当前结构风险低，建议无 iframe 直接 return）。

- [views/student/CourseSquare.vue:302-341/478-479] recruiting 计入 hasMSData 但模板分支只渲染 goldFeatured/featured（:303/:322）→ square 接口仅返回招募数据时 hasMSData=true，区块显示标题但内容空白，v-else 空态（:343）也不触发。
- [views/student/CourseSquare.vue:766-779 + 650-658] keyword watch 与 handleReset/handleSearch 未互斥：handleReset 清空 keyword 触发 watch 的延迟 fetch，同时自身立即 fetch → 每次重置 2 次请求；URL 带 keyword 刷新时 bindToQuery 设值亦与 onMounted 双触发。
- [views/student/CourseSquare.vue:667-671] 切换 page-size 时 handleSizeChange 置 page=1，若原页码>1，el-pagination 的 current-change 会再次触发 handlePageChange → 同一次操作 2 次请求。
- [views/student/CourseSquare.vue:415/432/542/385/395/398] 死代码：userRole、newestCourses、formatDate 定义后未使用；swrCache 死 import；TrendCharts/HomeFilled import 后模板未用。
- [views/student/CourseDetail.vue:597-599] isMobile ref 模板从未使用（死代码），却挂了一个模块级全局 resize 监听且永不移除（监听泄漏）。
- [views/student/CourseDetail.vue:468/873 与 472/649] slidesLoading、teacherLoading 只赋值不渲染，死状态。
- [views/student/CourseDetail.vue:387/403] 写评价/回复弹窗固定 width=480px，375px 屏横向溢出（无响应式覆盖）。
- [views/student/CourseDetail.vue:85] `course.avgRating.toFixed(1)` 直接调用，无 Number() 防御（CourseSquare 对同一字段用 Number(r).toFixed(1)）；后端返回字符串 avgRating 时将抛 TypeError 中断该节点渲染（当前后端返回 number，属潜在风险）。
- [views/student/CourseDetail.vue:655] checkEnrollment 调 getMyEnrollments() 不传 size → 选课记录超过后端默认 20 条时可能查不到本课程 → 已选课用户误显示"加入学习"（点击后由 409 兜底，自愈型误判）。
- [views/student/BundleDetail.vue:189/193-199] 支付成功后外层 getMyEnrollments 失败会落入外层 catch 弹"操作失败"——支付已成功却被提示失败，诱导重试造成重复订单（需网络抖动触发）。

- [views/auth/Login.vue:260-262] 网络错误双 toast：拦截器（request.js:102-108）已弹"网络连接异常/请求超时"，组件 catch 再弹一条措辞不同的"网络连接失败" → 断网时两条堆叠提示。
- [views/auth/Login.vue:221-228] 演示账号守卫只校验 VITE_DEMO_ADMIN_USER/PASS，教务处/教师/学生角色的 env 未配置时点对应 tag 把 undefined 填入表单（仅开发环境 + env 配半时触发）。
- [views/auth/Login.vue:238-242] handleLogin 的 loading 在 validate 异步回调内才置位（对比 handleRegister:272-273 守卫后立即置位）→ 快速连点存在两请求都过守卫的窗口，注释声称的 P2 修复不彻底。
- [views/auth/Login.vue:151] 页脚硬编码中文（`微课管理平台 · Powered by Vue 3 + Element Plus`），en-US 下不国际化。
- [views/public/MicroSpecialtySquare.vue:36/57/91/116 及全文 5-121] 公开页正文全硬编码中文（学校重点推荐/了解详情/招生中/暂无微专业项目等），未走 i18n，en-US 下整页中文。

- [components/common/SignatureUploader.vue:3-9] readonly/disabled 未作用于预览区"更换/删除"按钮（:6/:7 无 :disabled），readonly 模式下仍可删除/更换签名（handleRemove 会 emit 清空已签名图片）。
- [components/users/TeacherApprovalDialog.vue:80-98] handleApprove/handleReject 无 :loading/防抖，快速双击并发提交两次 updateTeacherStatus；驳回（teacherStatus=2 不可逆）无确认弹窗。
- [components/course/SectionEditDialog.vue:93-94] 提交乐观关闭：emit("submit") 后同步 handleClose，父组件异步失败时弹窗已关、表单已丢（叠加 P1-3 残留）。
- [components/common/DynamicTableEditor.vue:92-100] watch 回写 `localData.value = v || []` 直接引用父级 props 数组，此后 addRow/removeRow/v-model 直接 mutation 父 prop（当前靠 storage 包装器 JSON 去重恰好打断回声，属隐患写法）。
- [components/users/UserSearchBar.vue:136] 死代码：handleSelectChange 定义后无任何引用（模板直接用 debouncedEmit）。
- [components/users/BatchImportDialog.vue:25-40/65-67] 无客户端文件类型/大小校验：仅 accept 软限制，handleFileChange 只存 file.raw；:limit="1" 未处理 on-exceed（已选文件后再选新文件被 EP 静默拒绝）。
- [components/course/SectionList.vue:11] `(section.sortOrder % 10000) || section.sortOrder`：sortOrder 恰为 10000 整数倍时显示原值而非余数，显示语义不一致（低概率）。
- [components/users/UserDetailCard.vue:63-68] formatTime 对非法日期输出 "NaN-NaN-NaN 00:00:00"（new Date(非法串) 后直接取 getFullYear()，无 isNaN 兜底）。

- [views/student/LearningCenter.vue:1104-1108] checkTodayStatus 的 catch 是复制粘贴错误：打卡状态查询失败时执行 `chartData.value = []` 并提示"学习趋势数据加载失败"——清空的是图表数据而非打卡状态，提示语与场景无关；checkedInToday 保持 false，已打卡用户会再次看到"今日打卡"按钮。
- [views/student/LearningCenter.vue:855-863] getRecent 中 getLearningProgress 请求后 currentChapter 恒被赋为 1（注释自认"P1-I: 暂不显示 DB ID"）→"第{chapter}章"永远显示"第1章"，整个 try 块是死代码且每次进页多一次无效网络请求。
- [views/student/LearningCenter.vue:1041 + TrainingCenter.vue:86] getMyEnrollments() 未传 size（后端默认 20）→ 选课 >20 门时统计/最近学习/推荐/热力图与训练中心列表全部静默截断。
- [views/student/TrainingCenter.vue:94-104] N×M 并发请求风暴：所有课程章节 + 所有章节练习全部 Promise.all 并行（课程多时一次性几十个并发请求，存在触发网关/服务端限流风险）。
- [views/student/TrainingCenter.vue:44-47] exerciseCount===0 的章节（"暂无练习"标签）仍整体可点击进入空练习页；:103 getExercises({chapterId, size:50}) 以 50 截断计数，>50 题时 exerciseCount 显示 50。
- [views/student/LearningCenter.vue:792] `sharedEnrollments ? { data: sharedEnrollments } : getMyEnrollments()`：sharedEnrollments 恒为数组（空数组也 truthy）→ else 分支是永远不执行的死代码。

- [views/student/LearningView.vue:21 + 180-186] `@show-notes="activeTab = 'note'"` 把 activeTab 置为不存在的 tab key（tabs 只有 course/announcement/discussion/exam）→ 点工具栏"笔记"按钮后顶部 4 个 tab 全部无高亮；tabs 数组在 setup 时用 t() 求值一次，运行时切语言后 tab 文案不更新（模板内其它 i18n 是响应式的）。
- [views/student/LearningView.vue:452/463 + components/learning-view/ChapterSidebar.vue] 课程级"学习统计"卡塞入的是全站统计：getStudyDays()/getTotalTime() 均无 courseId 参数 → 与卡片所在课程语境不符。
- [components/learning-view/ChapterSidebar.vue:56] 课时时长直接渲染原始秒值（如 845），未像 VideoSection/VideoPlayer 那样 formatTime；:21-24 chapters 为空数组时无空态提示（el-collapse 直接空白）。
- [components/learning-view/VideoSection.vue:244-252] toggleFullscreen 用全局 `document.querySelector('.video-container')`（与 VideoPlayer 类名相同），选择器脆且非组件自引用（当前仅单实例可用）。
- [views/student/VideoPlayer.vue + composables/useVideoLearningData.js:126-147] switchChapter 只 push chapterId query，reloadVideo() 仍拉原 videoId 的视频（内容不变仅上下文变化，易误导）；且把目标章节 isCompleted 重置为 false，与"完成态不落库"叠加后完成勾选极易丢失；loadChapters 亦全量重置 isCompleted。
- [views/student/VideoPlayer.vue:819-842] 播放器笔记存 localStorage（useVideoLocalState），学习页 NotesPanel 笔记走后端 /api/note → 同一课程笔记在两个学生入口不互通（双轨）。

- [views/academic/Dashboard.vue:458-459] loadStats 失败 catch 静默 → stats 保持空对象，displayStats 停留 0/0%，无任何用户提示（其余 4 个 loader 均有 error flag 文案）。
- [views/academic/Dashboard.vue:437-456/466-481] 院系下钻时 loadStats 与 loadDepartmentStats 各自调用一次 getDepartmentDetail（同参数），每次刷新产生 2 份重复请求。
- [views/academic/EnrollmentOverview.vue:188-195/101-108] 改每页条数时若 el-pagination 同步调整 current-page 会同时触发 size-change 与 current-change → handleSizeChange 与 handlePageChange 各调一次 fetchData（一个 page=1、一个用旧 page），并发竞态可能短暂显示错误页码数据。
- [views/academic/EnrollmentOverview.vue:197-202] 死代码 formatDate（模板全部走全局 $formatDateTime，函数无调用方）。

- [views/academic/MicroSpecialtyReview.vue:28-29] 同一列同时写 :formatter="$formatDateTime" 与 #default 插槽渲染 createdAt（插槽优先，formatter 死代码且格式不一致）；:185-201 handleReopen 的 catch 只判 e==='cancel'，点 X/Esc（'close'）误报"操作失败"；:98/127-128 hasAccess 在 setup 时一次性快照 role，fetchData 对无权限静默 return → getInfo 异常路径下页面永久空列表且无提示。
- [views/academic/MicroSpecialtyProposalReview.vue:107/31] nextTick 死导入；空态 CTA @click 跳 '?tab=ALL' 但组件从不读 route.query.tab（activeTab 固定 ref('PENDING')，:115）→ 跳转后回到同一 PENDING 空列表，"查看全部"为死按钮。
- [views/academic/MicroSpecialtyClassImport.vue:27/46/125/138] 班级选项 `${c.studentCount || 0}` 恒显示 0 人（ClassVO 无 studentCount）；v-if="result.errors && result.errors.length" 死代码（后端 MicroSpecialtyClassImportResultVO 无 errors 字段，真实明细在 failedList 弹窗）；getMicroSpecialtyList/getClasses 各只取前 100 条 → 第 100 条之后的班级无法导入。
- [views/academic/MicroSpecialtyFeaturedReview.vue:29-32/101] "全部" tab 列出从未申请置顶的微专业（featuredStatus='NONE'），statusMap 无 NONE 映射 → 状态标签直接显示原始英文代码 "NONE"。
- [views/academic/StorageApplicationReview.vue:30-31] :formatter="$formatDateTime" 与 #default 插槽重复渲染 updatedAt，formatter 死代码。

- [views/student/MyCourses.vue:840-843 vs 831-836] handleH5TabChange 不调用 loadVideoProgress（PC 的 handleTabChange 调用）→ H5 在首次加载完成前切走 Tab 后视频进度永不加载；:795-828 videoProgressLoaded 一次性标志 → 退课重选/新加入课程后视频进度不刷新。
- [views/student/MyCourses.vue:701/566/777] totalVideos 恒 0 死数据（LearningProgressVO 无 totalVideos 字段，仅 completedVideos）；totalElements 赋值后从未使用（模板用 totalDisplayElements）→ 死代码。
- [views/student/MyCourses.vue:880-910 + EnrollmentLifecycleServiceImpl.java:436-441] 进度>50% 的课程后端拒绝退课，前端仍显示"退课"按钮且确认文案只区分是否付费 → 学生确认后才收到拒绝提示。
- [views/student/Checkout.vue:151/156] `order.courseName` 字段不存在（OrderVO 为 courseTitle）→ 恒 undefined（靠 items[idx].title 兜底）；:15-16/66 el-col :span 16/8 无 :xs 响应式 + 600px 结果弹窗 → 375px 破版。
- [views/student/StudentFavorites.vue:24] alt 插值 item.title 恒 undefined（CourseFavoriteVO 字段为 courseTitle），图片 alt 文本残缺。

## 已核实无问题的文件
- views/student/BundleSquare.vue、MyOrders.vue（0 基分页、PageResult 契约、状态枚举、确认弹窗+loading 均正确）
- components/learning-view/ChapterSidebar.vue、ExerciseQuickPanel.vue、ResourceToolbar.vue
- components/common/DatePickerYear.vue、SignatureBlock.vue、RichTextWithCounter.vue、components/storage/DatePickerYM.vue、components/users/UserTable.vue、components/course/SectionList.vue
- views/student/AchievementWall.vue（三态齐全、导入无误、无确凿缺陷）
- components/profile/PasswordEditor.vue、UserInfoEditor.vue
- 全组生命周期 hooks import、$t/$formatDateTime 全局（main.js:46-47 + vue-i18n globalInjection）、api export 名逐一比对均无缺失（除上文 P0/P1 指出的两处）。

## 优先级排序（建议修复顺序）
1. P0-1 MicroSpecialtyDetail t/i18nT（白屏级）
2. P0-2 MyCourses getLearningProgress 未 import（静默功能失效）
3. P1 收藏删除错接口（数据误删风险）
4. P1 VideoPlayer/SlidePlayer 进度与完成态不落库（核心学习数据）
5. P1 LearningCenter streak=0、正确率全 0（学生端核心指标失真）
6. P1 Exams _attempted/startTime（状态机死代码）
7. P1 CourseDetail 评价分页回显、Settings saveTimer/quietHours
8. P1 教务处字段错配组（EnrollmentOverview/Dashboard/CrossDept/ClassImport/GoldManage）
9. P1 死功能组件（TeacherApprovalDialog/BatchImportDialog 无入口、SectionEditDialog 残留）
10. P2 体验项（按移动端破版 > 空态/提示 > 死按钮排序）