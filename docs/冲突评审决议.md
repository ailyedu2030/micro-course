# 冲突评审决议

> 日期：2026-06-11
> 触发事件：Phase 0 启动时发现 11 份真文档之间存在 5 个字段级冲突
> 决策者：总工程师
> 性质：本文档是**字段冲突的最终裁决**，后续代码与 skill 必须 100% 对齐

---

## 1. 裁决总表

| # | 冲突项 | 文档 A（弃） | 文档 B（采纳） | **最终裁决** | 落地动作 |
|---|--------|------------|------------|-----------|---------|
| C1 | REST 路径前缀 | 权限矩阵 v1.0 = `/api/v1/` | API契约-Phase1 v1.1 = `/api/` | **`/api`（无前缀）** | 权限矩阵 v1.0 → v2.0，全部 `/api/v1/` → `/api/` |
| C2 | 响应 message | 开发规范 v1.2 §3.3.1 = `"success"` | API契约-Phase1 v1.1 = `"ok"` | **`"ok"`** | 开发规范 v1.2 → v1.3，§3.3.1 示例改 `"ok"` |
| C3 | 分页字段 | 开发规范 v1.2 §3.3.1 = `items/total/page/pageSize` | API契约-Phase1 v1.1 §1.3 = `items/page/size/totalElements/totalPages` | **`items/page/size/totalElements/totalPages`** | 开发规范 v1.2 → v1.3，§3.3.1 分页示例改 5 字段 |
| C4 | `/me` 路径 | 权限矩阵 v1.0 = `/users/me` | API契约-Phase1 v1.1 = `/auth/me` | **`/api/auth/me`** | 权限矩阵 v1.0 → v2.0，全部 `/users/me` → `/auth/me` |
| C5 | 表名前缀 | 项目结构规范 v1.0 §4.2 = `sys_user` 示例 | 数据字典 v0.4 = `users` | **`users`（无前缀）** | 项目结构规范 v1.0 → v1.1，删除 `sys_` 前缀示例 |

---

## 2. 裁决理由（每条独立说明）

### C1 · REST 路径前缀

**裁决：`/api`（无前缀）**

理由（按权重排序）：
1. **代码现状已用 `/api`**：5 个现有 Controller 全部 `@RequestMapping("/api/...")`，改前缀会破 100% 调用
2. **API契约 v1.1 是 Phase 1 直接契约**：18 个 API 全部用 `/api`（44 处一致）
3. **Phase 1 阶段不需要 v1 标识**：项目未对外发布、无版本兼容性需求
4. **后续若需版本化**：用 `/api/v2` 升级路径而非 URL 前缀

**受影响文档**：权限矩阵 v1.0（141 处 `/api/v1/`）

### C2 · 响应 message

**裁决：`"ok"`**

理由：
1. **API契约 v1.1 是 18 个 API 的直接契约**，54 处一致使用 `"ok"`
2. **开发规范 §3.3.1 是"标准格式示例"**，未指定具体字面值
3. **国际惯例**："ok" 比 "success" 更短、与 HTTP 200 含义一致

**受影响文档**：开发规范 v1.2 §3.3.1

### C3 · 分页字段

**裁决：`items/page/size/totalElements/totalPages`**

理由：
1. **API契约 v1.1 §1398 修订记录**："修复分页格式（统一为 items）"
2. **`totalElements + totalPages` 组合避免前端二次计算** `Math.ceil(total/size)`
3. **Spring Data 生态标准字段名就是 `totalElements`**（Page 接口）
4. **与 18 个 API 的 5 处分页响应示例完全一致**

**受影响文档**：开发规范 v1.2 §3.3.1

### C4 · `/me` 路径

**裁决：`GET /api/auth/me`**

理由：
1. **auth 是身份域、users 是业务域**，`me` 属于"获取当前身份"而非"获取用户业务对象"
2. **与 C1 路径前缀一致**（`/api`）
3. **与 5 个现有 Auth 控制器设计空间一致**（`@RequestMapping("/api/auth")`）
4. **RESTful 语义更清晰**：`/auth/me` = 我的身份；`/users/{id}` = 任意用户

**受影响文档**：权限矩阵 v1.0（3 处 `/users/me`）

### C5 · 表名前缀

**裁决：`users`（无前缀）**

理由：
1. **现有 4 个 Entity 全部无前缀**（`@TableName("users")` 等）
2. **数据字典 v0.4 是 38 张表的权威定义**，全部无 `sys_` 前缀
3. **4 张核心表保持零前缀一致性最高**：`users` / `departments` / `majors` / `classes`
4. **`sys_` 是 Spring 生态历史习惯**，但本项目已不用

**受影响文档**：项目结构规范 v1.0 §4.2

---

## 3. 修订清单（与 G2-G4 子工单一一对应）

| 子工单 | 文档 | 版本变更 | 修订内容 |
|--------|------|---------|---------|
| G2 | `docs/权限矩阵.md` | v1.0 → **v2.0** | 141 处 `/api/v1/` → `/api/`；3 处 `/users/me` → `/auth/me`；头部版本号；修订记录 |
| G3 | `docs/开发规范.md` | v1.2 → **v1.3** | §3.3.1 message `"success"` → `"ok"`；§3.3.1 分页字段 → 5 字段；§14.1-14.3 路径更新到 `/Users/jackie/微课平台/` |
| G4 | `docs/项目结构规范.md` | v1.0 → **v1.1** | §4.2 命名表 `sys_user` → `users`；§4.4 路径示例 → `/api/users` |

---

## 4. 守护规则（写进地基宪法 v1.0）

```
1. 任何后续 Phase 必须先查本文档，确认字段是否已裁决
2. 字段冲突若再次出现，必须先在本文档中新增裁决行，再写代码
3. 未在本文档登记的字段冲突 = 阻塞合并
4. 任何"以代码为准"的反推行为 = 禁止
```

---

## 5. 新增裁决（Phase 5-9 期间）

| # | 冲突项 | 文档 A（弃） | 文档 B（采纳） | **最终裁决** | 落地动作 |
|---|--------|------------|------------|-----------|---------|
| C6 | 徽章表结构 | V18 migration = 单表 `badges`（user_id/badge_type/badge_name） | 数据字典 v0.5 §9.0-§9.1 = 双表 `badge_definitions` + `achievements` | **双表方案**：V37 `badge_definitions`(元数据) + V38 `achievements`(用户获得记录) | V18 `badges` 标记为废弃，V40 迁移数据 + DROP |
| C7 | 系统配置表名 | 数据字典 v0.4 = `system_configs` | V12 migration = `admin_settings` | **`admin_settings`**：与代码已实现的 AdminSetting Entity、AdminSettingsController、AdminSettings.vue 一致 | 数据字典 v0.5 已修正为 `admin_settings` |
| C8 | 视频表字段名 | V5 migration = `file_name` / `hls_url` | 数据字典 v0.5 = `original_name` / `m3u8_url` | **`original_name`/`m3u8_url`**：V25 migration 已执行 RENAME COLUMN | 数据字典保持 v0.5，V25 已对齐 |

## 6. 关联文档

- `docs/复盘/根因报告-Phase0.md`（5 层根因 + 5 条防范）
- `docs/复盘/地基工程-v1.0-终验.md`（执行后填写）

---

*文档版本：v1.1*
*最后更新：2026-06-12*
*维护者：总工程师*

---

## 7. 2026-06-22 · Phase A-4 权限注解对账（P0-9）+ 端点补齐（P0-5）

> 触发：Phase 1 报告 P0-5（30 端点未实现）+ P0-9（49% 权限注解与矩阵 v2.0 偏差）
> 基线：`docs/权限矩阵.md` v2.0 为唯一真相
> 约束：UX 零退化（现有**合法**请求不得突然 403）；不实现 PermissionEvaluator（Phase C）；不改 DB schema

### 7.1 已应用的权限收紧（本次落地，零回归 / 矩阵明确越权）

| # | 端点 | 矩阵依据 | 旧注解 | 新注解 | 决策原因 | 影响用户 |
|---|------|---------|--------|--------|---------|---------|
| A1 | PUT `/api/courses/{id}` | §2.3 UPDATE_OWN_COURSE（AC ❌） | `hasAnyRole('TEACHER','ADMIN','ACADEMIC')` | `hasAnyRole('TEACHER','ADMIN')` | ACADEMIC 越权（§3.4 统计只读，无内容编辑权）；前端无 academic 调用 updateCourse | 仅 ACADEMIC 受限（零回归） |
| A2 | POST `/api/courses/{id}/publish` | §2.3 PUBLISH_COURSE（仅 A）+ §4.2 | `hasAnyRole('TEACHER','ADMIN','ACADEMIC')` | `hasRole('ADMIN')` | TEACHER/ACADEMIC 越权发布；前端 publish 仅 CourseApproval.vue（与已 ADMIN-only 的 approve/reject 同视图）调用 | TEACHER/ACADEMIC 失去发布权（按矩阵应如此） |
| A3 | PUT `/api/enrollments/{id}` | §2.8 UPDATE_ENROLLMENT_STATUS（AC ❌） | `hasAnyRole('TEACHER','ADMIN','ACADEMIC')` | `hasAnyRole('TEACHER','ADMIN')` | ACADEMIC 越权改选课状态；前端无 .vue 调用 updateEnrollment | 仅 ACADEMIC 受限（零回归） |

### 7.2 已盘点但**本次不收紧**（避免 UX 回归 / 待 Phase B/C）

| 端点 | 矩阵 | 现注解 | 不收紧原因 |
|------|------|--------|-----------|
| PUT `/api/courses/{id}/status` | A only | `ADMIN,ACADEMIC` | 前端 CourseList.vue「下架」按钮显式对 ACADEMIC 可见并调用本端点 → 收紧会触发真实用户 403，违反 UX 零退化。待产品确认 academic 下架权后处理 |
| DELETE `/api/courses/{id}` | §2.3 T(草稿)/A，AC ❌ | `ADMIN,ACADEMIC` | 需确认前端是否对 academic 显示删除按钮；删除为破坏性操作，保守延后 |
| `/api/teaching-classes/*` 写操作（create/update/delete/addStudent/...） | §2.9 多为 A only | 含 TEACHER/ACADEMIC | 存在 `/teacher/teaching-classes` 专属视图（role=TEACHER,ADMIN）+ `/admin/teaching-classes`（role=ADMIN,ACADEMIC）→ 教师/教务处为既有产品功能，收紧将破坏现网。需前端联调 |
| chapter/video/question/exercise 内容 CUD 的 ADMIN | §2 表列 A ✅，§4.2 注 A 不可编辑内容 | `TEACHER,ADMIN` | §2 表与 §4.2 存在矛盾；§2 表授予 ADMIN，移除将破坏管理后台。待 §2-vs-§4.2 裁决（Phase C 由 PermissionEvaluator 做 owner 级判定） |
| 所有「T(创建者)」owner 校验（update/chapter/video/question/exercise/submit 等） | §3.2 | 仅角色级 | 需 `@permissionEvaluator.isCourseOwner` SpEL，但该 Bean 未实现（Phase C 任务）；本次不引入半成品 owner SpEL，owner 校验维持 Service 层既有模式 |

### 7.3 P0-5 新增端点（8 个，路径以矩阵 v2.0 §1 为准，统一 `R<T>` / `PageResult`）

| 端点 | @PreAuthorize | owner/本人校验 |
|------|---------------|----------------|
| GET `/api/courses/pending-review` | `hasRole('ADMIN')`（§1.3 + §2.3 仅 A） | — |
| GET `/api/courses/{id}/students` | `hasAnyRole('TEACHER','ADMIN','ACADEMIC')` | TEACHER→assertCourseOwnership |
| GET `/api/videos/{id}/progress` | `hasRole('STUDENT')` | userId=当前用户（防 IDOR） |
| POST `/api/videos/{id}/progress` | `hasRole('STUDENT')` | 同上；归属服务端推导 |
| GET `/api/exercises/{id}/result` | `hasAnyRole('STUDENT','TEACHER','ADMIN')` | STUDENT→仅本人记录 |
| GET `/api/exercises/{id}/attempts` | `hasRole('STUDENT')` | userId=当前用户 |
| GET `/api/teaching-classes/{id}/schedule` | `isAuthenticated()`（§2.9 全角色） | — |
| GET `/api/enrollments/{id}` | `hasAnyRole('STUDENT','TEACHER','ADMIN','ACADEMIC')` | STUDENT→本人 / TEACHER→课主 |

> 验证：`NewEndpointsSecurityTest` 10 用例全 PASS；回归 54 用例全 PASS；mvn compile / precheck 通过。

---

*Phase A-4 维护者：接口层契约对齐智能体*
