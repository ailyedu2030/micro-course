# 周度复盘 · 2026-W30 Phase 6 教师模块

> 复盘时间：2026-07-25
> 复盘范围：Phase 6 教师模块从收口开发到 PR 合并
> 复盘人：AI 总工程师 / 项目执行负责人
> 关联交付：PR #123，主线提交 `812269c4`

---

## 一、完成 vs 计划

| 事项 | 计划节点 | 实际状态 | 偏差 |
|---|---|---|---|
| 教师模块剩余功能收口 | W30 内 | 已完成 | 无 |
| 补齐关键测试与文档 | W30 内 | 已完成 | 无 |
| 第 9 节任务执行与验收回写 | W30 内 | 已完成 | 无 |
| PR / CI / Bot / merge 闭环 | W30 内 | 已完成 | 有一次 CI 回归，但当日修复闭环 |
| 进入发布准备判断 | 合并后 | 已完成 | 无 |

**偏差结论：本阶段主计划完成，唯一偏差来自 1 个 CI 不稳定测试，已在同轮修复。**

---

## 二、做得好的地方

1. 没有停在“功能完成”，而是把测试、文档、PR、CI、主线同步一起收口
2. 第 9 节任务不只是计划，而是回写成了可审计的执行与验收章节
3. CI 失败后没有绕过，而是直接定位根因并做最小修复
4. 本地主线同步没有采用破坏性覆盖，而是先做备份再安全对齐远端

---

## 三、问题与根因

### 3.1 CI 首轮失败

- 症状：
  - PR #123 首轮 `backend` 失败，导致 `auto-approve` 一并失败
- 直接原因：
  - `TeacherCourseCoverIntegrationTest` 把课程列表顺序写死为 `items[0].id == 1`
- 根本原因：
  - 测试假设了固定返回顺序，但集成环境中课程排序并不保证目标课程恒为第一项
- 处置：
  - 将断言改为按 `id == 1` 过滤目标课程后再校验封面字段
- 防止再发：
  - 后续同类集成测试避免对无契约保证的列表顺序做硬编码断言

### 3.2 浏览器联调工具不稳定

- 症状：
  - `ego-browser` 当前会话接入失败
  - Playwright 本地浏览器二进制也不稳定
- 影响：
  - 本轮真实浏览器联调证据获取存在噪音
- 处置：
  - 改用组件回归测试 + 本地隔离部署 + 已有联调证据补齐闭环
- 结论：
  - 当前问题属于工具环境噪音，不构成业务阻断

---

## 四、本轮新增教训

1. 集成测试不要假设无契约保证的返回顺序
2. 文档闭环要与代码闭环同批完成，否则很容易出现“代码已收口，状态台账还停留在计划”的脱节
3. 主线同步要优先保历史，再做对齐，避免用破坏性方式覆盖本地提交链

---

## 五、遗留项与后续动作

| 类别 | 事项 | 当前状态 | 下阶段动作 |
|---|---|---|---|
| 发布执行 | `deploy-gate` 检查 | 已完成 | 门禁窗口有效，已完成 production 发布 |
| 发布执行 | `ROLLBACK_PLAN.md` / `CHANGELOG` / `deferred-items` 核对 | 已完成 | 继续保留为回滚与审计依据 |
| 发布执行 | production 执行记录 / 观察窗口 | 已启动 | 持续跟进 24 小时观察并输出发布后复盘 |
| 风险治理 | `vendor-el` 大包体 warning | 持续治理 | 转入性能治理专题 |
| 风险治理 | `Entity-数据字典漂移` advisory | 持续治理 | 转入数据契约治理专题 |

---

## 六、复盘结论

本轮 Phase 6 教师模块已经完成从开发、测试、文档、PR 到 production 发布的闭环，当前项目主线与生产环境均处于健康状态。

但复盘也给出一个明确边界：

- 当前结论是“已完成生产发布并进入观察窗口”
- 不是“任务已经彻底结束、可以停止跟踪”

下一阶段必须继续完成 24 小时观察、输出发布后复盘，并把持续治理项纳入后续迭代排期。
