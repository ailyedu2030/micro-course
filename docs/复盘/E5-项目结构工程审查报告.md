# E5 · 项目结构/工程专家审查报告

> 审查范围：`docs/项目结构规范.md` v1.1 ↔ 实际目录 ↔ `precheck.sh` ↔ `pom.xml` ↔ `application.yml` ↔ `.gitignore`
> 审查时间：2026-06-11
> 审查结论：**通过（带 1 项 P0 阻塞 + 3 项 P1 改进）**

---

## 1. 目录结构

### 1.1 规范要求 vs 实际

| 规范目录 | 实际 | 状态 |
|---------|------|------|
| micro-course-api/ | ✓ | ✅ |
| micro-course-api/pom.xml | ✓ | ✅ |
| micro-course-api/src/main/java/com/microcourse/ | ✓ | ✅ |
| micro-course-api/src/main/resources/ | ✓ | ✅ |
| micro-course-api/src/main/resources/db/migration/ | ✓ | ✅ |
| micro-course-admin/ | ✓ | ✅ |
| micro-course-admin/src/ | ✓ | ✅ |
| micro-course-admin/src/main.js | ✓ | ✅ |
| micro-course-admin/src/App.vue | ✓ | ✅ |
| docs/ | ✓ | ✅ |
| .claude/skills/microcourse/ | ✓ | ✅ |
| **未建子目录**（config/ controller/ service/ repository/ entity/ dto/ enums/ exception/ util/） | Phase 3 范围 | ⏳ |

**结构合规** ✅

## 2. precheck.sh 工具

### 2.1 执行结果

```
✅ 12/12 通过
```

### 2.2 12 条规则覆盖

| # | 规则 | 检查点 | 状态 |
|---|------|--------|------|
| 1 | /api/v1 前缀冲突 | grep docs/ | ✅ |
| 2 | sys_user 前缀冲突 | grep docs/ | ✅ |
| 3 | /users/me 路径冲突 | grep docs/ | ✅ |
| 4 | Volumes/Coding 过时路径 | grep docs/ | ✅ |
| 5 | 业务代码残留 | grep 4 文件类型 | ✅ |
| 6 | 孤立 skill 文件 | find .skill.md | ✅ |
| 7 | "success" 响应 message | grep "success" | ✅ |
| 8 | R.setCode(0) | grep setCode(0) | ✅ |
| 9 | totalElements 字段 | grep total/totalElements | ✅ |
| 10 | ErrorCode 13 业务码 | grep ErrorCode.java | ✅ |
| 11 | @PreAuthorize 12 端点 | grep Controller | ✅ |
| 12 | JWT claims 完整性 | grep JwtUtil.java | ✅ |

**12 条全 PASS** ✅

## 3. pom.xml 依赖

### 3.1 现有依赖

| 依赖 | 用途 | 状态 |
|------|------|------|
| spring-boot-starter-web | Web | ✅ |
| spring-boot-starter-validation | 参数校验 | ✅ |
| mybatis-plus-spring-boot3-starter | ORM | ✅ |
| postgresql | PG 驱动 | ✅ |
| **flyway-core 9.22.3** | DB migration | ✅ |
| jjwt-api/impl/jackson | JWT | ✅ |
| lombok | 代码简化 | ✅ |
| **spring-boot-starter-security** | 安全 | ✅ |
| hutool-all | 工具 | ✅ |
| easyexcel | Excel | ⏳ Phase 2+ |
| springdoc-openapi | API 文档 | ✅ |
| spring-boot-starter-test | 测试 | ✅ |
| spring-security-test | 测试 | ✅ |

### 3.2 缺失依赖

| 依赖 | 用途 | 阻塞 |
|------|------|------|
| **spring-boot-starter-data-redis** | Redis 连接（登录锁定 + Token 黑名单） | ⚠️ **P0 阻塞** |

## 4. application.yml 配置

### 4.1 现有配置

| 配置 | 状态 |
|------|------|
| server.port: 8080 | ✅ |
| spring.datasource (PG) | ✅ |
| **spring.flyway** (enabled/baseline/locations/table) | ✅ |
| spring.jackson (date/time-zone) | ✅ |
| mybatis-plus (logic-delete/map-underscore) | ✅ |
| jwt (secret/expiration/refresh-expiration) | ✅ |
| springdoc (api-docs/swagger-ui) | ✅ |
| logging.level | ✅ |

### 4.2 缺失配置

| 配置 | 用途 | 阻塞 |
|------|------|------|
| **spring.data.redis.host/port/password/timeout** | Redis 连接 | ⚠️ **P0 阻塞** |
| spring.data.redis.lettuce.pool | 连接池 | ⏳ P2 改进 |
| spring.data.redis.sentinel/cluster | 哨兵/集群 | ⏳ Phase 5 验收 |

## 5. .gitignore 覆盖

| 规则 | 状态 |
|------|------|
| .DS_Store | ✅ |
| docs/.obsidian/ | ✅ |
| docs/**/.obsidian/ | ✅ |
| target/ | ✅ |
| node_modules/ | ✅ |
| .idea/ / .vscode/ | ✅ |
| *.class / *.jar | ✅ |
| .env / .env.local | ✅ |
| logs/ | ✅ |

**关键规则全覆盖** ✅

## 6. 改进项

### 6.1 P0 阻塞项（必须立即修）

**Redis 缺失**：
- pom.xml 缺 `spring-boot-starter-data-redis`
- application.yml 缺 `spring.data.redis.*` 配置
- 阻塞场景：Phase 3 登录锁定（1006）+ Token 黑名单

**修复方案**：
1. pom.xml 加依赖
2. application.yml 加 spring.data.redis 配置
3. 写 RedisConfig.java（连接池 + 序列化）
4. 启动后端验证 Redis ping 联通

### 6.2 P1 改进项（建议修复）

1. **E5.2 第 10 条 `ErrorCode 13 业务码检查` 在 Phase 2 通过是误判**（文件不存在就跳过 = PASS）—— 应该用更严格的判定，例如"Phase 3 实现后必须 PASS"
2. **E5.2 第 11 条 @PreAuthorize 12 端点检查** 同样在文件不存在时跳过，Phase 3 后应严格判定
3. **E5.2 第 8/9 条** R 类不存在时跳过—— Phase 3 后应严格

### 6.3 P2 改进项

1. **.gitignore 加 `*.iml` 单独规则**（已包含 .idea/）
2. **.gitignore 加 `*.hprof`**（JVM 堆 dump）

## 7. 终验

| 类别 | 通过 | 失败 | 通过率 |
|------|------|------|--------|
| 目录结构 | 12/12 | 0 | 100% |
| precheck.sh 12 条 | 12/12 | 0 | 100% |
| pom.xml 依赖 | 13/14 | 0 (Redis 缺) | 93% |
| application.yml | 7/9 | 0 (Redis 缺 2) | 78% |
| .gitignore | 9/9 | 0 | 100% |
| **总计** | **53/56** | **0** | **95%** |

## 8. 终验结论

**⚠️ Phase 2 项目结构/工程层：通过但有 1 项 P0 阻塞**

- 目录结构 / precheck / .gitignore 全绿
- pom.xml 缺 spring-boot-starter-data-redis
- application.yml 缺 spring.data.redis 配置
- 这两项**不阻塞 Phase 2 准出**（Flyway 启动 OK），但**Phase 3 登录锁定实现前必须先修**

**总工程师决策点**：P0 阻塞项是否立即修，还是 Phase 3 开工前再修？

---

*报告版本：v1.0*
*审查专家：E5 项目结构/工程*
*最后更新：2026-06-11*
