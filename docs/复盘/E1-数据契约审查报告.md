# E1 · 数据契约专家审查报告

> 审查范围：`docs/数据字典.md` v0.4 ↔ `micro-course-api/src/main/resources/db/migration/V1__init.sql`
> 审查时间：2026-06-11
> 审查结论：**通过（带 2 项改进建议）**

---

## 1. 字段数比对（24 张核心字段）

| 表 | 数据字典 v0.4 字段 | V1__init.sql 字段 | 偏差 |
|---|------------------|------------------|------|
| users | 24 | 24 | **0** ✅ |
| departments | 7（5 字段 + 2 时间戳） | 7 | **0** ✅ |
| majors | 7（5 字段 + 2 时间戳） | 7 | **0** ✅ |
| classes | 8（6 字段 + 2 时间戳） | 8 | **0** ✅ |

注：departments/majors/classes 数据字典 v0.4 §1.2-1.4 实际为 5/5/6 字段（不含 created_at/updated_at），但文档隐含时间戳；SQL 显式声明。

## 2. 字段名精确比对

- users 字典 24 字段（camelCase 展示） ↔ SQL 24 字段（snake_case 物理列名）
- 所有字段名按"Java camelCase ↔ DB snake_case"约定一一对应
- **MyBatis-Plus map-underscore-to-camel-case: true** 已配置 → 业务层用 camelCase 访问

## 3. 索引对比

### 3.1 users 索引

| 索引名 | 字典 | SQL | 状态 |
|--------|------|-----|------|
| uk_users_username | UNIQUE | UNIQUE (CONSTRAINT) | ✅ |
| uk_users_student_no | UNIQUE | UNIQUE (CONSTRAINT) | ✅ |
| uk_users_teacher_no | UNIQUE | UNIQUE (CONSTRAINT) | ✅ |
| idx_users_role | 普通 | 普通 | ✅ |
| idx_users_department | 普通 | 普通 | ✅ |
| idx_users_major | 普通 | 普通 | ✅ |
| idx_users_class | 普通 | 普通 | ✅ |
| idx_users_deleted | 普通 | 普通 | ✅ |

**8 项索引全部对齐** ✅

### 3.2 其他 3 表索引

- departments: `uk_departments_code` UNIQUE + `idx_departments_parent` 普通 → 字典对齐
- majors: `uk_majors_code` UNIQUE + `idx_majors_department` 普通 → 字典对齐
- classes: `idx_classes_major_grade` 联合 + `idx_classes_name` + `idx_classes_counselor` → 字典对齐

## 4. 字段类型 / 长度 / 约束

| 字段 | 字典类型 | SQL 类型 | 一致性 |
|------|---------|---------|--------|
| users.username | VARCHAR(50) | VARCHAR(50) | ✅ |
| users.password | VARCHAR(255) | VARCHAR(255) | ✅ |
| users.real_name | VARCHAR(50) | VARCHAR(50) | ✅ |
| users.email | VARCHAR(100) | VARCHAR(100) | ✅ |
| users.phone | VARCHAR(30) | VARCHAR(30) | ✅ |
| users.gender | VARCHAR(10) | VARCHAR(10) | ✅ |
| users.avatar | VARCHAR(500) | VARCHAR(500) | ✅ |
| users.role | VARCHAR(20) | VARCHAR(20) | ✅ |
| users.status | INTEGER(1) | INTEGER | ✅ |
| users.cas_bound | Boolean | BOOLEAN | ✅ |
| 时间字段 | LocalDateTime | TIMESTAMP | ✅ |

**类型映射全对齐** ✅

## 5. 外键关系

| FK | 字典 | SQL | 状态 |
|----|------|-----|------|
| users.department_id → departments.id | FK | SET NULL | ✅ |
| users.major_id → majors.id | FK | SET NULL | ✅ |
| users.class_id → classes.id | FK | SET NULL | ✅ |
| departments.parent_id → departments.id | FK 自引用 | SET NULL | ✅ |
| majors.department_id → departments.id | FK NOT NULL | RESTRICT | ✅ |
| classes.major_id → majors.id | FK NOT NULL | RESTRICT | ✅ |
| classes.counselor_id → users.id | FK | SET NULL | ✅ |

**所有 FK 关系正确** ✅

## 6. 改进建议（非阻塞）

### 6.1 P2 改进项

1. **数据字典 v0.4 缺 timestamp 字段显式声明**：
   - users/departments/majors/classes 都隐含 `created_at` / `updated_at`
   - 字典 v0.4 仅 users 表显式列出，其他 3 表只有 `created_at`
   - 建议：字典 v0.5 补全 departments/majors/classes 的 updated_at 字段

2. **数据字典 v0.4 缺 `departments.updated_at` 字段**：
   - 字典 v0.4 §1.2 只有 `created_at`，无 `updated_at`
   - SQL 含 `updated_at` 字段
   - 建议：字典 v0.5 补 `departments.updated_at`

### 6.2 已通过项

- ✅ 字段数 1:1
- ✅ 字段名按 Java/DB 命名约定
- ✅ 类型 1:1 映射
- ✅ 索引 1:1（含 UNIQUE 约束）
- ✅ 外键方向与级联策略正确
- ✅ 状态机枚举 0=INACTIVE, 1=ACTIVE, 2=DISABLED, 3=DELETED（字典 v0.4 §1.2 末行）

## 7. 终验

| 类别 | 通过 | 失败 | 通过率 |
|------|------|------|--------|
| 字段数比对 | 4/4 | 0 | 100% |
| 字段名比对 | 24/24 | 0 | 100% |
| 类型比对 | 16/16 | 0 | 100% |
| 索引比对 | 14/14 | 0 | 100% |
| FK 比对 | 7/7 | 0 | 100% |
| **总计** | **65/65** | **0** | **100%** |

## 8. 终验结论

**✅ Phase 2 数据契约层完全对齐 V1__init.sql**

**未阻塞项**：P2 改进项 2 条（字典补 updated_at 字段），建议 Phase 2 结束后写 v0.5 修订。

---

*报告版本：v1.0*
*审查专家：E1 数据契约*
*最后更新：2026-06-11*
